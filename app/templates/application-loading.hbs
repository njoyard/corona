<div class="layout-row layout-fill layout-align-center-center">
  <div class="loading-container">
    <PaperProgressCircular
      @diameter={{96}} />
    <h1>Starting up...</h1>
    {{#if this.loadingState}}
      <i>{{this.loadingState}}</i>
    {{/if}}
  </div>
</div>
